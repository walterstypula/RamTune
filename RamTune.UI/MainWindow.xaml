<Window x:Class="RamTune.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:RamTune.UI"
        xmlns:local="clr-namespace:RamTune.UI"
        xmlns:views="clr-namespace:RamTune.UI.Views"
        mc:Ignorable="d"
        Title="RamTune"
        Width="900"
        AllowsTransparency="False"
        Background="#252526"
        Height="540"
        xmlns:viewmodels="clr-namespace:RamTune.UI.ViewModels">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:RomEditorVM}">
            <views:RomEditor />
        </DataTemplate>
    </Window.Resources>
    <DockPanel>
        <views:ActionMenu DockPanel.Dock="Top" DataContext="{Binding .}" />
        <ContentControl Content="{Binding ViewContext}" />
    </DockPanel>
</Window>